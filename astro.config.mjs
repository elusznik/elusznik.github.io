// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // TODO: Replace with your GitHub username and repo name
  site: 'https://elusznik.github.io',
  base: '/',
  outDir: './docs',
});
